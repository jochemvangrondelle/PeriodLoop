import React from "react";
import { Box, Text } from "native-base";

const Calendar: React.FC = () => (
  <Box borderWidth={1} borderColor="gray.300" p={4} borderRadius="md">
    <Text>Calendar Placeholder</Text>
  </Box>
);

export default Calendar;
